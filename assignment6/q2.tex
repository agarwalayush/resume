\documentclass{article}

\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\begin{document}
\pagestyle{empty}


% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=blue!20, 
    text width=4.5em, text badly centered, node distance=3cm, inner sep=0pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!20, 
    text width=5em, text centered, rounded corners, minimum height=4em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [draw, ellipse,fill=red!20, node distance=3cm,
    minimum height=2em]
    
\begin{tikzpicture}[node distance = 3cm, auto]
    % Place nodes
    \node [cloud] (init) {Start of Pre-Registration};
    \node [block, below of=init, node distance=3cm] (req) {Request for course};
    
    \node [block, left of=req, node distance=3cm, yshift= -1cm] (another) {choose another course};
    \node [decision, below of=req, node distance=3cm] (accept) {If accepted?};
    \node [block, below of=accept, node distance = 3cm] (attend) {Attend the classes};
    \node [decision, below of=attend, node distance=3cm] (sleep) {Does Instructor make you sleep?};
    \node [block, below of=sleep, node distance=3cm] (midsem) {Midsem};
    \node [decision, below of=midsem, node distance=3cm] (marks) {Midsem marks upto your expectation};
    \node [block, right of=marks, node distance=3cm] (drop) {Drop};
    \node [block, below of=marks, node distance=3cm] (endsem) {Endsem};
    \node [cloud, right of=endsem, node distance=3cm] (over) {Over};
    % Draw edges
    \path [line] (pre) -- (req);
    \path [line] (req) -- (accept);
    \path [line] (another) |- (req);
    \path [line] (accept) -| node [near start] {no} (another);
    \path [line] (accept) -- node {yes}(attend);
    \path [line] (attend) -- (sleep);
    \path [line] (sleep) -- (midsem);
    \path [line] (midsem) -- (marks);
    \path [line] (marks) -- (drop);
    \path [line] (marks) -- (endsem);
    \path [line] (drop) -- (over);
    \path [line] (endsem) -- (over);
    \path [line] (sleep) -| node {yes}(drop);
\end{tikzpicture}


\end{document}